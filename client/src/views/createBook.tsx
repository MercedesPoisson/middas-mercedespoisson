import React from "react";
import withLayout from "../components/ui/withLayout";

const CreateBook = () => {
    return (
        <div>
            Hola soy la ruta del formulario createBook
        </div>
    )
}

export default withLayout(CreateBook);