import React from "react";
import withLayout from "../components/ui/withLayout";

const Favorites = () => {
    return (
        <div>
            Hola soy la ruta de favoritos
        </div>
    )
}

export default withLayout(Favorites);